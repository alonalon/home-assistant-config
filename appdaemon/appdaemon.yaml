secrets: /conf/secrets.yaml
appdaemon:
  logfile: STDOUT
  errorfile: STDERR
  threads: 10
  app_dir: /conf/apps
  plugins:
    HASS:
      type: hass
      ha_url: !secret ha_url
      token: !secret ha_key
